#coding: utf-8

from setuptools import setup,find_packages

setup(
    version = 0.1,
    packages = ['coursera'],
    description = "libs for bio",
    url = "https://github.com/dougc333/bioinformatics",
    install_requires =
    [
        "numpy"
    ]
)
